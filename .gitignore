# Client work: every company's audits live in its own <company>-report/ folder and stay out of git.
# Anchored to the project root so the skill folder .claude/skills/cro-funnel-report/ is not caught.
/*-report/

# Python
__pycache__/
*.pyc

# OS / local Claude settings
.DS_Store
.claude/settings.local.json
.claude/worktrees/
